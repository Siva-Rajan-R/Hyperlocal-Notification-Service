services:
  notification-service:
    build: 
      context: .
      dockerfile: .
    
    env_file:
      - .env
    
    networks:
      - backend-networks
    
    ports:
      - "8009:8000"
    
    restart: unless-stopped